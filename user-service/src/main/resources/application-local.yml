spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8070/realms/voyagrr/protocol/openid-connect/certs

  datasource:
    url: jdbc:postgresql://localhost:5434/user_service_database
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

keycloak:
  keycloak-url: http://localhost:8070
  realm: voyagrr
  client-id: voyagrr-client
  client-secret: NlimILlMn12VE1HL2aUG4o1CAfWFfF7A